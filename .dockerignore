# 빌드 산출물 (이미지 안에서 새로 빌드함)
build/
.gradle/

# 프론트 의존성·빌드본 (Stage 1에서 새로 설치·빌드함)
frontend/node_modules/
frontend/build/

# git / IDE / OS
.git/
.idea/
*.iml
.vscode/
.DS_Store

# 로컬 전용 설정 (Railway는 환경변수로 주입)
docker-compose.yml
